#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running TypeScript check before push..."
cd NextJS/Apps && npx tsc --noEmit

if [ $? -ne 0 ]; then
  echo "❌ TypeScript check failed. Fix errors before pushing."
  exit 1
fi

echo "✅ TypeScript check passed!"
